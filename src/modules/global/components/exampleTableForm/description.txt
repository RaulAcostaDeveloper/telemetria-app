Este componente sirve como ejemplo para crear un formulario ya sea para CREAR o EDITAR

Es necesario usar este componente como ejemplo para ACTIVAR EL BOTÖN y para DEFINIR LA FUNCIÓN

En el primer useEffect se DEFINE LA FUNCIÓN

Se muestra un ejemplo de cómo ACTIVAR EL BOTÓN cuando los valores cumplen cierta validación

El dataObject?: { [key: string]: string | number }; es una propiedad que se puede obtener porque el formulario de EDITAR lo requiere pero el de CREAR no
Solo hay que traer dataObjet para extraer el id, y el contenido actual del objeto para rellenar los campos